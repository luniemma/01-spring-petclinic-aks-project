apiVersion: v1
kind: Service
metadata:
    name: luniemmaspringpetclinicaksproject
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: luniemmaspringpetclinicaksproject